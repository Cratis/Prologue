You are refining the names in an event model that was mechanically extracted from a running system's observed behavior.
The provisional names are generic CRUD placeholders derived from HTTP verbs and database operations (Create/Update/Delete verbs, Created/Updated/Deleted/Changed suffixes). Your job is to replace them with the intentful domain language a domain expert would use, inferred from the observed behavior — not to rubber-stamp the CRUD names.
Rules:
- Reject generic CRUD names. Do not keep a command named Create*, Update*, Delete*, or Handle*, and do not keep an event named *Created, *Updated, *Deleted, or *Changed, unless the evidence genuinely reveals no more specific domain intent.
- Commands are imperative and express business intent (for example RegisterAuthor, PlaceOrder, ReserveBook) — name the intent, not the HTTP verb (not CreateAuthor).
- Events are past-tense facts naming what happened in the domain (for example AuthorRegistered, OrderPlaced, BookReserved) — name the fact, not the database operation (not AuthorCreated).
- Infer the intent from the evidence: the request path, the tables and columns that changed, and the telemetry span names reveal what the behavior actually accomplishes.
- Modules and features name domain areas. Read models, projections, and properties are PascalCase.
- Keep a provisional name unchanged only when it is already intentful domain language. A CRUD placeholder never qualifies.

Refinement examples (provisional -> refined):
- CreateAuthor -> RegisterAuthor, and AuthorCreated -> AuthorRegistered
- CreateOrder -> PlaceOrder, and OrderCreated -> OrderPlaced
- UpdateAddress -> ChangeShippingAddress, and AddressUpdated -> ShippingAddressChanged
- DeleteReservation -> CancelReservation, and ReservationDeleted -> ReservationCancelled

Observed behavior (evidence):
{EVIDENCE}
Provisional names to refine:
{NAMES}

Respond with ONLY a single JSON object mapping each provisional name to its refined name. Do not add commentary.
