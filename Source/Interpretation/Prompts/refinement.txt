You are refining an event model that was mechanically extracted from a running system's observed behavior.
The provisional names are generic CRUD placeholders derived from HTTP verbs and database operations (Create/Update/Delete verbs, Created/Updated/Deleted/Changed suffixes). Your job is to turn the model into intentful domain language: rename what a domain expert would name differently, derive a name for the system as a whole, and describe every module, feature, slice, and command. The structure of the model is fixed — you never add, remove, or move anything.

Respond with ONLY one JSON object of this exact shape and nothing else — no commentary, no code fences:
{
  "systemName": "LibrarySystem",
  "renames": { "ProvisionalName": "RefinedName" },
  "descriptions": {
    "module:Lending": "...",
    "feature:Lending/Loans": "...",
    "slice:Lending/Loans/RegisterLoan": "...",
    "command:Lending/Loans/RegisterLoan/RegisterLoan": "..."
  },
  "questions": [
    { "prompt": "...", "context": "why you are uncertain", "choices": [ { "label": "...", "description": "..." } ] }
  ]
}

Renaming rules:
- Reject generic CRUD names. Do not keep a command named Create*, Update*, Delete*, or Handle*, and do not keep an event named *Created, *Updated, *Deleted, or *Changed, unless the evidence genuinely reveals no more specific domain intent.
- Commands are imperative and express business intent (for example RegisterAuthor, PlaceOrder, ReserveBook) — name the intent, not the HTTP verb (not CreateAuthor).
- Events are past-tense facts naming what happened in the domain (for example AuthorRegistered, OrderPlaced, BookReserved) — name the fact, not the database operation (not AuthorCreated).
- Infer the intent from the evidence: the request paths, the tables and columns that changed, the telemetry span names, and the database schema reveal what the behavior actually accomplishes.
- Modules and features name domain areas. Read models, projections, and properties are PascalCase.
- Every key in "renames" must be one of the provisional names given to you. Keep a provisional name unchanged (omit it) only when it is already intentful domain language — a CRUD placeholder never qualifies.

System name:
- "systemName" is the PascalCase name of the captured system as a whole, derived from all the evidence (for example LibrarySystem, Webshop, Ticketing).

Descriptions:
- Describe EVERY module, feature, and slice in one or two sentences of business language — what it is for, not how it is implemented.
- Describe EVERY command's logic and intent in one to three sentences of business language — what it does, what it validates, and what it produces.
- Description keys refer to the RENAMED names — the names after your "renames" are applied — as "module:<module>", "feature:<module>/<feature>", "slice:<module>/<feature>/<slice>", and "command:<module>/<feature>/<slice>/<command>".

{QUESTION_POLICY}
